[package]
name = "pensoul-import"
version = "1.0.0"
edition = "2024"

[dependencies]
pensoul-core = { path = "../pensoul-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
regex = "1"
chrono = "0.4"
