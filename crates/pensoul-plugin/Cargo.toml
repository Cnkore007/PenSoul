[package]
name = "pensoul-plugin"
version = "0.1.0"
edition = "2024"

[dependencies]
pensoul-core = { path = "../pensoul-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "2"
