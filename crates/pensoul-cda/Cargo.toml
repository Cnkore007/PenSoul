[package]
name = "pensoul-cda"
version = "1.0.0"
edition = "2024"

[dependencies]
pensoul-core = { path = "../pensoul-core" }
petgraph = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
