# 更新日志

> **发版约定（强制）：** 每次发布新版本（含预发布）必须在此文件追加对应版本的更新记录，
> 并与 [README.md](README.md) 和 GitHub Releases 正文保持同步。格式遵循
> [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [1.0.2] - 2026-08-03

### 新增

- 智能分卷：大纲页新增「智能分卷」按钮，按基础设定中的「预计卷数」把全部章节按顺序均分到
  「第一卷 / 第二卷 …」，每卷可折叠、内含章节细纲；未填写卷数时保留导入/手动卷名，不自动分卷
- 字数硬约束：写作 prompt 明确每章目标字数 ±15% 允许区间（目标 3200 字 → 2720-3680 字），
  严禁注水或压缩；审查阶段新增字数检查维度，且引擎在正文字数超出 ±15% 时强制拦截重写
- 一键全部删除：世界观、人物志、大纲、笔耕四个页面新增「全部删除」按钮，
  可分别一键清空全部世界观条目、全部角色、全部卷/章节/脉络节点、全部章节（保留卷结构），均带二次确认

### 修复

- 笔耕删除按钮常显：章节导航的删除按钮不再依赖悬停才出现，直接可见可用
- 细纲不计入章节数与字数：项目列表、概览、基础设定、大纲统计统一只统计已开始细写
  （有正文）的章节，大纲页底部区分显示「已写 N 章 · 细纲 M 章」

## [1.0.1] - 2026-08-03

### 新增

- 细纲一键全部展开：脉络节点「全部展开」由后台任务按批自动续展到节点全部章节，批间携带已展开细纲作为上下文衔接，保证长程连续
- 细纲展开跨页恢复：展开任务挂后端控制面（运行旗标/进度/取消/事件缓冲），切换页面后回到大纲页自动重连恢复运行状态与进度
- 讨论作者确认增强：补充意见可单独「提交意见」保存（不触发生成），新增「重新讨论」按钮清空结果重新发起
- 模型默认设置：模型设置页可把某个模型设为全局默认，蒸馏/优化/细纲展开等未指定模型的环节优先使用默认模型

### 修复

- 讨论裁决预算不足：裁决由「一次裁决全部分歧」改为逐条独立调用（单条输出短），单条失败不再拖垮整批；分维度提炼预算 8192 → 16384
- 模型开关持久化：手动关闭的模型不再因配置 API Key 而在切换页面后重新点亮
- 大纲卷展开状态持久化：卷展开/折叠状态随卷元数据落盘，切换页面后保持
- 蒸馏技能加载：技能内容编译进应用，运行时不再扫描用户主目录（避免 nuwa-skill 副本遮蔽项目内技能）
- 大纲页界面优化：卷头层级、章节行悬停反馈、操作按钮收纳、状态文字徽标

### 其他

- 修复笔耕去 AI 味重写与章节删除相关历史问题（详见 1.0.0 后提交记录）

## [1.0.0] - 2026-08-02

### 新增

- Harness 确定性流程引擎：Stage/Gate/WAL 驱动创作流程，支持自动/人工/条件三种门控模式与崩溃恢复
- 工作流模板化：全局模板库 + 项目引用分层，模板可增删执行环节并绑定模型与写作技法卡
- 章节连写管线：章前策划 → 写作 → 异模型门控审查 → 回灌自动连写，支持暂停/继续/停止
- 黄金三章硬门控：钩子/爽点子分数不达标不放行
- 四层记忆系统：热/温/冷/叙事四级记忆，8 步更新管道，预算按编辑模式分配
- CDA 一致性驱动架构：影响图 + BFS 传播，千章规模构建 < 50ms
- 多 Agent 讨论：立论/交锋两轮交锋、按维度并行提炼、冲突标记与独立裁判裁决
- 全链路批注：笔耕行内/整章 + 细纲/脉络/人物志/世界观实体级批注，批注中心聚合管理
- 受控保存（保存并审核）：保存前 LLM 判定有效性并评估全文影响，二次确认后落库
- 版本历史与逐级撤回：编辑前快照 + 前后端格式转换，连续撤回显示剩余次数
- 编辑经验沉淀：diff 采样 + LLM 有效性判定，经验带来源注入后续审查 prompt
- 墨韵（文风 + 审校 + 规则配置）：反 AI 味规则词表/计分/语言铁律可在线编辑并即时生效
- 反 AI 味检测：五类规则（套话/弱化副词/书面连接词/意义膨胀/情绪直说）0-100 分
- 细纲展开：情节脉络节点一键展开全部逐章细纲，可中途取消、已展开部分保留
- 三层蒸馏：专家蒸馏、书籍蒸馏、方法论蒸馏
- 并发控制：乐观锁 + 操作队列 + 冲突检测
- 增量一致性检查：章节/卷/全书范围增量扫描，5 条规则
- LLM 输出容错：JSON 报错位置驱动式修复，章节正文剥离规划文本与代码围栏

### 修复

- 作品简介保存到独立字段并正确显示
- 撤回回退到编辑前状态（编辑前快照 + 前后端格式转换）
- 版本号前后端历史一致，回滚版本号回到目标版本

### 其他

- 首个公开发布版本，支持 macOS / Windows / Web 三种分发形态
