# PenSoul 2.0 — AGENTS.md

> 本文件是 PenSoul 2.0 项目的 AI Agent 协作规范。所有在本仓库工作的 Agent 必须遵守。

## 核心理念

**让引擎管流程，让模型管创作。AI 无权跳步。**

- 流程由引擎硬性驱动，AI 不可跳步
- AI 产物默认为"建议制"，用户确认后才写入正典
- 一切设计以 500 万字级长篇的一致性为第一优先级
- `NovelOntology` 是唯一正典，记忆/影响图/一致性都是派生状态

## PenSoul 2.0 三个根本转变

1. **流程控制 → 约束边界**：引擎定义"不能做什么"，而非"必须按什么顺序做"
2. **记忆存储 → 信息检索**：系统预测用户需要什么信息，而非被动等待查询
3. **事后检测 → 事前预防**：在修改时预测影响，在创作时检查一致性

## 语言规范

- 注释、提交信息、文档：**一律中文**
- 标识符：保持英文
- 提交格式：`<类型>: <中文描述>`

## 强制禁令

1. 不要绕过集成层——章节保存后必须走 `on_chapter_saved`
2. 不要用旧模拟器 `harness_exec.rs`——新功能走 `pipeline/`
3. 不要用 ModelRouter——LLM 调用统一走 `llm_helper`
4. 不要硬编码密钥——API key 只存于 `data/_config/llm-config.json`（由 `pensoul-infra::llm` 统一读写，接口脱敏）
5. 不要静默映射——解析不了的 ID 显式跳过，禁止当第 0 章
6. 不要手写 `unsafe`——除非必要且注释说明
7. 不要凭记忆写文档——必须与代码事实一致
8. 不要用 GET 执行写操作——副作用接口一律 POST/PUT/DELETE（防 CSRF；审计 2026-08-13 沉淀）

## 详细规则文档索引

| 主题 | 文档 |
|---|---|
| 设计体系（书斋雅韵） | `docs/DESIGN.md` |
| 2.0 重新设计（愿景与路线图） | `docs/REDESIGN.md` |
| 知识库化重构设计（P0-P6 冻结稿） | `docs/design/2026-08-13-知识库化重构设计.md` |
| 前沿技术调研（Agent 写小说） | `docs/research/2026-08-12-agent写小说前沿技术调研与融入建议.md` |
| 会话工作日志（按时间戳归档） | `docs/session-logs/INDEX.md` |

## 提交与推送（用户通知制，强制）

- **提交**：任何代码/文档变更完成后**必须**提交：`git status`（确认无敏感文件进暂存——`data/`、`node_modules/`、`docs/` 已被 .gitignore 排除）→ `git add -A` → `git commit -m "<类型>: <中文描述>"`
- **推送**：**只有用户明确通知才执行 `git push origin main`**，不得主动推送；用户说"推送/上传/发到 GitHub"等即通知
- 推送凭据由 gh CLI 自动提供（keyring OAuth），无需交互；push 失败（网络/认证）必须显式报告，禁止静默跳过
- 环境事实：gh CLI 位于 `/opt/homebrew/bin/gh`（非交互 shell 的 PATH 不含 Homebrew 目录，需要时 `export PATH=/opt/homebrew/bin:$PATH`；git 的 credential helper 已由 gh 配置好，push 自动生效）
- 远程仓库：`origin = https://github.com/Cnkore007/PenSoul`（main 分支）
- **`docs/` 不入库**（.gitignore 排除）：设计文档、会话日志等仅存本地，作为跨会话进度的唯一事实来源；远程 GitHub 不承载任何 docs 内容

## 会话日志规范（强制）

- **任何代码变更**（新增/修改/删除文件，含前端、后端、测试、配置）完成后**必须归档**：把"做了什么"总结为 Markdown 写入 `docs/session-logs/`，并追加到 `docs/session-logs/INDEX.md`——日志是跨会话进度的唯一事实来源，不归档等于进度丢失
- 项目文档变更（本文件、`docs/` 下设计稿/调研等）同样必须归档
- 纯咨询/问答/查询类交互（无代码或文档变更）**不强制归档**，避免索引噪音
- 文件名带时间戳：`YYYY-MM-DD-HHMM-主题.md`（如 `2026-08-09-2154-对抗性审计全部修复.md`）
- 内容至少包含：任务目标、关键决策与取舍、改动文件清单、验证结果（测试/实证）、遗留事项与待验证假设
- 新会话接手项目时，先读本规则与 `docs/session-logs/INDEX.md` 获取共同上下文，再开始工作
- 日志必须与代码事实一致，禁止凭记忆编造未发生的改动

## 实现与数据规范（强制）

### 禁止假数据

- 所有功能必须真实实现并落盘：禁止 mock 数据、占位返回、假成功、写死演示数据
- UI 展示的数据必须来自真实存储（`NovelOntology` 正典或配置文件），禁止在前端伪造
- `TODO + 恒返回通过`、`unwrap_or_default` 掩盖真实错误等"空壳实现"视为假数据，一律不允许

### 真实增删改查

- 用户描述"要增加 / 删除 / 修改"某功能时，必须**前后端一起实现**：后端 API + 持久化 + 前端交互，缺一不可
- 任何写操作必须保存到正典/配置文件，禁止只改内存、只改界面、只返回 ok 不落盘
- 新增与删除必须走统一数据链路（ontology 变更 → 派生状态重建 → 落盘），禁止绕过集成层

### 跨页面问题排查

- 修复某个页面/模块的问题时，必须主动检查其他页面是否存在**相同模式**的问题（同类表单字段、同类状态机、同类调用方式、同类错误处理）
- 发现同类问题必须一并修复，禁止只修一处留隐患
- 检查结果与"哪些页面已排查"必须写入会话日志

### 经验总结与复用

- 每次任务完成必须把可复用经验（根因、解法、模式、坑）写入 `docs/session-logs/` 的日志中
- 开始新任务前先读 `docs/session-logs/INDEX.md` 与相关日志，查询是否已有解决办法
- 遇到重复问题（同类报错、同类缺陷、同类需求）时，**先搜索日志与代码**，有现成方案直接复用，禁止重复踩坑
- 经验积累为规则或检查项时，同步更新本文件，保持规则与事实一致

### 本地开发启动（用户语义约定）

- 用户说"打开 web 客户端 / 客户端 / 项目"时，必须**前端 + 后端一起启动**，缺一不可：
  - 前端：Vite 开发服务器（`npm run dev`），端口 `1420`，`/api` 由 Vite 代理到后端
  - 后端：`cargo run --bin pensoul-server`，监听 `127.0.0.1:3001`
- 只开前端不开后端 = 页面能打开但所有数据请求失败，不视为完成"打开"
- 后端源码有改动时用 `cargo run`（或重新构建）运行最新代码，不能直接跑过期的 `target/debug/pensoul-server` 二进制

## 当前实现状态（与代码事实一致，2026-08-13 核对）

### 数据与架构

- `NovelOntology` 是唯一正典；图谱、约束引擎、记忆管线均为派生状态，写操作后 `rebuild_derived` 重建
- 章节正文唯一写路径：`save_chapter_content` → `update_content` → `rebuild_derived` → `on_chapter_saved`（修订历史 + 一致性评分）→ `save_project`；AI 生成类功能（初稿/续写/审校/改写/级联/批量）一律**建议制**不落盘，写回必须经集成层
- 项目 ID 白名单（字母/数字/`_`/`-`，防路径穿越，handler 与持久化双层校验）；**项目标题支持中文**；服务只监听 `127.0.0.1:3001`，CORS 只放行本机前端源，全局 panic 兜底（CatchPanicLayer）
- 副作用接口全部 POST/PUT/DELETE；远端文档抓取校验 http/https 且拒绝回环/私网地址（防 SSRF），流式读取限 5MB

### 引擎能力

- 约束引擎：角色状态倒流/时间线倒流/设定重名/伏笔状态机/事件悬空引用五条硬规则 + 章节/伏笔/批注状态机门控，写路径执行 `pre_edit_check` 事前检查与 `post_edit_validate` 事后验证（失败回滚）
- 记忆管线：实体摘要/细节 + 时间/情感上下文，预算按意图参与分配（`pensoul-memory`）
- 事件总线：`on_chapter_saved` 发布事件（当前无订阅者，待功能落地补充）

### LLM 与 Agent

- LLM 调用统一走 `llm_helper`（`pensoul-infra::llm`），密钥只存 `data/_config/llm-config.json`（接口脱敏，不明文出接口）；全局配置支持增删改查/默认/测试/上下文检测/模型文档拉取与参数摘取
- Agent 体系（P0b）：writer / reviewer / extractor / outliner / distiller 五角色，每角色可绑定 LLM 配置，未绑定回退全局默认；配置存 `_config/agent-config.json`
- 结构化输出（`json_mode=true`）必须用 `structured_output_tokens` 给预算（至少 4096，长文本至少 8192）：思考型模型的 reasoning tokens 会计入 max_tokens，硬编码 2000-3000 会截断 JSON（2026-08-14 实测 kimi-k3）
- 事实提取提示词必须携带与本章正文相关的档案实体清单；`character_update` 只用于已建档角色，未建档角色统一 `new_character`（解析端兼容 `{"facts":[...]}` 与裸数组）

### 创作管线（P0-P6 已落地，前端对应视图）

- **萌芽**（对话式诘问创作台）：session/start/chat/generate/apply/discard/clear
- **图谱 · 知识库**：人物/组织/设定/事件/伏笔/规则六类档案全量增删改查（事实提取自动写入 + 手动维护）
- **大纲**：脉络 CRUD、章节 CRUD、正文读写；细纲分块生成（每 12 章一批，注入全书上下文）、细纲导入（章号上限 = 当前最大 +500 防 DoS，并回写 `expanded_until` 进度）
- **笔耕**：正文编辑、AI 初稿/续写（记忆检索 + 硬约束 + 技巧注入 + 消痕检测）、AI 审校（本地启发式 + LLM 深度，`llm_error` 区分降级原因）、批注 CRUD（状态机：新建→已指派→已解决）、AI 审核/消痕改写（diff 确认）、级联同步（仅向后 + 上限 20 章）、批量写作（每批 3 章检查点制，仅选「已有细纲且为空」的章节；单章/批量/级联保存均触发事实提取）
- **仪表盘**：统计卡 + 创作流水线阶段 readiness（萌芽定盘/人物档案/世界观/大纲/细纲/批量写作/审校发布）与下一步动作
- **事实提取（P1）**：保存后自动触发 → 结构化 Fact → 写档案（冲突告警）→ 操作日志（审计轨迹，可回滚）
- **书籍蒸馏（P3）**：语料摄取 txt/md/epub/pdf → 7+1 维度风格画像 → `_config/style-recipe.json` → 注入写作/审校提示词（版权红线：无风格锚、禁抄原句）；正文缺失的书跳过并返回 `missing_books`
- **归档（P6）**：操作日志（500 条）/ 级联日志（200 条）、回滚（≤100 条，日志写失败显式 `log_warning`）、压缩归档、成本报告
- 前端视图：作品库 / 仪表盘 / 萌芽 / 图谱 / 大纲 / 笔耕 / 约束 / 设定

### 未实现（属 REDESIGN 路线图，不视为现有能力）

- `pensoul-workflow`、`pensoul-collab`、`pensoul-orchestration` 尚未实现；「事件发布」无消费者、DNS 重绑定型 SSRF 无法静态拦截（已如实注释）

## 品牌

- **名称**：PenSoul
- **标语**：「让引擎管流程，让模型管创作」
- **许可证**：MIT
