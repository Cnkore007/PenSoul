# data/ —— 运行时数据目录（不入库）

本目录保存运行时产生的数据，**不纳入版本控制**：

- `_config/`：LLM API 密钥（`llm-config.json`）、Agent 配置、风格配方、操作日志等 —— 含敏感信息，严禁提交
- `projects/`：各作品的 NovelOntology 正典（用户创作数据）

如需迁移/备份，直接复制本目录即可。所有配置与数据均可由运行中的应用重新生成或迁移。
