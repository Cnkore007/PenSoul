# agent.md — PenSoul 仓库 Agent 强制工作约定

> 本文件对**所有**在本仓库工作的 LLM / Agent 工具生效，无论你叫什么名字、来自哪家厂商、以什么形式运行。
> 这是仓库级的强制规则，优先级高于任何工具的默认行为（低于用户明确的指令）。

## 一、开工前必读（不读不许动工）

1. 先读 `AGENTS.md`（全局规范：中文注释、提交信息格式、清理习惯、安全红线）。
2. 再读 `docs/PROGRESS.md`（本仓库的工作记忆：进度、问题、设计思考）。
3. 涉及接口/命令时读 `docs/IPC-CONTRACT.md`；涉及架构时读 `docs/ARCHITECTURE.md`。
4. 执行 `git status`，确认工作区是否有用户未提交的改动；**不要覆盖、不要擅自提交**。

## 二、每轮工作结束后（强制写入）

在 `docs/PROGRESS.md` 追加一条记录，必须包含：

- **日期与主题**：`## YYYY-MM-DD：<一句话主题>`
- **改动范围**：涉及的文件/模块（列出路径）
- **遇到的问题**：现象、原因、如何排查（不写"没问题"）
- **设计思考**：关键决策、备选方案、为什么这么选
- **状态**：进行中 / 已完成 / 待验证
- **下次待办**：未完成的事，留给下一个 Agent 接续

规则：

- **每轮都要写**，哪怕改动很小；"没有值得写的问题"也要写清楚为什么。
- **接续上轮**：如果上轮留下"下次待办"，本轮必须回应它。
- **不允许**只更新代码不更新 PROGRESS.md；这不影响你完成任务，但任务完成后必须补上。

## 三、代码变更后（文档必须同步）

以下情况视为"代码变更"，在收尾前必须同步对应文档：

| 变更 | 必须同步的文档 |
|---|---|
| 新增/修改/删除 IPC 命令或事件 | `docs/IPC-CONTRACT.md`（命令表 + 事件协议） |
| 架构级改动（crate 职责、数据流、持久化、新子系统） | `docs/ARCHITECTURE.md` + `docs/DEVELOPMENT.md` |
| 用户流程变化（新功能入口、流程顺序、交互变化） | `docs/WORKFLOW.md` |
| 构建/测试/规范变化 | `docs/DEVELOPMENT.md` |
| 任何上述情况 | `docs/PROGRESS.md` 追加记录 |

**文档必须以代码事实为准**：禁止凭记忆、凭旧文档、凭猜测写内容；不确定就 `rg` 查代码。

### 文档时间戳（强制）

- 每个文档标题下方的首行必须带时间戳：`> 最后更新：YYYY-MM-DD`。
- **每次修改任何文档，必须同步把头部时间戳改成当天日期**；不改时间戳 = 文档未更新。
- 新建文档时必须带上创建当天的时间戳。
- `docs/PROGRESS.md` 的每条记录本身必须写日期（`## YYYY-MM-DD：<主题>`），头部时间戳同步为最后追加日。

## 四、硬性红线（违反即失败）

1. **不泄漏密钥**：`data/_config/api-keys.json` 含真实 API key；任何代码、文档、提交、日志不得出现密钥明文。
2. **不动用户未提交的改动**：`git status` 里的修改属于用户，覆盖前必须先确认；不擅自 `git commit` / `git reset` / `git stash drop`。
3. **不提交构建产物与临时文件**：`target/`、`node_modules/`、`__pycache__/`、`.DS_Store`、测试产物一律清理或 gitignore，用完即删。
4. **不执行用户未要求的网络请求/安装**：除非构建或测试必需（如 `cargo fetch`）。
5. **不用旧入口做新功能**：新功能走 `pipeline/`，不沿用 `harness_exec.rs`；应用层 LLM 调用走 `llm_helper`，不直接用 ModelRouter。
6. **大文件不入库**：>1MB 的文件考虑 git-lfs 或外部存储。

## 五、提交信息规范（如需提交）

格式：`<类型>: <中文描述>`，如 `feat: 新增章节连写管线`、`fix: 修复保存章节版本冲突`、`docs: 重建架构文档`。一个逻辑变更一个提交，提交前确认工作区无未纳入的临时文件。

## 六、收尾检查清单

- [ ] `docs/PROGRESS.md` 已追加本轮记录
- [ ] 涉及的命令/事件已同步 `docs/IPC-CONTRACT.md`
- [ ] 涉及架构/流程/构建的改动已同步对应文档
- [ ] 无密钥、无临时文件、无未清理的测试产物
- [ ] `git status` 中没有意外产生的文件（新文件都是有意的）
